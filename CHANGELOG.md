# 迭代记录

本项目采用语义化版本号记录功能演进。版本按从新到旧排列。

## v2.0.0 — 编辑器专用双 Skill

### 背景

同一组 LaTeX 定界符在 Obsidian 与 Typora 中表现不同。统一转换会让一个编辑器显示正常、另一个编辑器出错，因此需要按目标编辑器分流。

### 新增

- 新增 `chatgpt-md-obsidian`：把 `\(...\)`、`\[...\]` 转换为 `$...$`、`$$...$$`。
- 新增 `chatgpt-md-typora`：保留 `\(...\)`、`\[...\]`。
- 两个 Skill 均包含独立的 `SKILL.md`、运行脚本、Codex 界面元数据、中文 README 和测试。
- 通用脚本新增 `--math-delimiters dollar|backslash` 参数。

### 保持不变

- 标题、粗体、多层列表、代码块、Markdown 表格、引用和链接继续按原逻辑清理。
- 围栏代码块内部内容不会参与公式定界符转换。
- 不增加网络请求、遥测或第三方 Python 依赖。

### 迁移

- 原 `$chatgpt-md` 用户如果主要使用 Obsidian，可改用 `$chatgpt-md-obsidian`。
- 主要使用 Typora 的用户改用 `$chatgpt-md-typora`。
- 旧入口继续保留，默认行为与 Obsidian 版一致。

## v1.1.0 — 标题与正文公式修复

### 修复

- 在普通正文、标题和列表中识别 ChatGPT 复制出的 `\(...\)` 与 `\[...\]`。
- 将其转换为 `$...$` 与 `$$...$$`，避免 LaTeX 源码被直接显示。
- 保持代码围栏中的公式示例不变。

## v1.0.0 — 初始版本

### 新增

- 从 macOS 剪贴板读取 ChatGPT HTML，失败时回退到纯文本。
- 恢复标题、强调、列表、多层列表、代码块、表格、引用、链接和 KaTeX/MathML 公式。
- 清理多余空行、不可见字符和常见 Unicode 项目符号。
- 将结果自动写回剪贴板，并提供只预览的 `--stdout` 模式。
